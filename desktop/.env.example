# REST base. Every request in src/api is relative to this.
VITE_API_BASE_URL=https://messanger.cognilabs.org

# WebSocket base. The client appends /ws/chat and the auth query itself.
VITE_WS_BASE_URL=wss://messanger.cognilabs.org
